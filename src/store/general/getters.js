export function leftSideDrawer(state) {
  return state.leftSideDrawer
}

export function rightSideDrawer(state) {
  return state.rightSideDrawer
}

export function leftSideDrawerOptions(state) {
  return state.leftSideDrawerOptions
}

export function currentUserType(state) {
  return state.currentUserType
}

export function notifications(state) {
  return state.notifications
}

export function notificationCount(state) {
  return state.notifications.length
}
